let initialState = {
    idea: {id: 1, itype: "IPO", iname: "Tesla", idescription: "Автомобили", iprice: "15,5"}
};

const ideaReducer = (state = initialState) => {
    return state;

}

export default ideaReducer;